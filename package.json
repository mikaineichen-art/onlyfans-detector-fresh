{
  "name": "onlyfans-detector",
  "version": "1.0.0",
  "description": "OnlyFans detection API for Railway",
  "scripts": {
    "postinstall": "playwright install chromium",
    "start": "gunicorn api_server:app --bind 0.0.0.0:$PORT"
  },
  "engines": {
    "node": "18.x"
  }
}
