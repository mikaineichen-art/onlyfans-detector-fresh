[phases.setup]
nixPkgs = ["python311", "chromium", "gcc", "glib", "nss", "nspr", "dbus", "atk", "cups", "drm", "xcb", "x11", "pango", "cairo"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium",
    "playwright install-deps"
]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "./start.sh"
